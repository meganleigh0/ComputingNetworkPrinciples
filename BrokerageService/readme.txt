This program simulates a Stock Brokerage Service using TCP/UDP sockets and RSA Encryption. 
To create executables run:
    make ALL

To clear the executables run:
    make clear

After making the files run in the following order:
    ./keymanage
    ./Broker
    ./Client

This program was tested on a GCP e2 VM instance. 
